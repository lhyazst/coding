<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zeus.dao.BagsMapper" >


  <sql id="Base_Column_List">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Wed Jan 16 14:47:00 CST 2019.
    -->
    id, user_id, serial_number, recycle_bin_serial_number, create_time, update_time,
    status, bagsType, recycle_bin_id, orc_id
  </sql>

  <select id="fandAll" resultType="com.zeus.entity.Bags">
    select
    <include refid="Base_Column_List"/>
    from bags where status = "1"

  </select>
  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultType="com.zeus.entity.Bags">
    select
    <include refid="Base_Column_List" />
    from bags
    where id = #{id,jdbcType=INTEGER}
  </select>
  <select id="selectorcid" parameterType="java.lang.Long" resultType="com.zeus.entity.Bags">
    select
    <include refid="Base_Column_List" />
    from bags
    where orc_id = #{id,jdbcType=INTEGER} and  status ="1"
  </select>

  <select id="selectusersbags" parameterType="java.lang.Long" resultType="com.zeus.entity.Bags">
    SELECT * FROM bags WHERE orc_id IN(
    SELECT id FROM orc WHERE recycle_user_id = #{userid,jdbcType=BIGINT}
    ) AND STATUS = "1"

  </select>



  <insert id="insert" parameterType="com.zeus.entity.Bags" >

    insert into bags ( user_id, serial_number,
    recycle_bin_serial_number, create_time, update_time,
    status, bagsType, recycle_bin_id,orc_id
    )
    values ( #{userId,jdbcType=BIGINT}, #{serialNumber,jdbcType=BIGINT},
    #{recycleBinSerialNumber,jdbcType=BIGINT}, #{createTime,jdbcType=TIMESTAMP}, #{updateTime,jdbcType=TIMESTAMP},
    #{status,jdbcType=VARCHAR}, #{bagstype,jdbcType=VARCHAR}, #{recycleBinId,jdbcType=BIGINT},#{orcId,jdbcType=BIGINT}
    )
  </insert>

  <select id="selecttow" parameterType="java.lang.Long" resultType="com.zeus.entity.Bags">
    select
    <include refid="Base_Column_List"/>
    from bags where  user_id= #{userid,jdbcType=INTEGER} and serial_number= #{orcNumber,jdbcType=INTEGER}

  </select>
  <update id="updateBagsStatus" parameterType="java.lang.Long" >

    update bags
    set
    status = "1"
    where id=#{id,jdbcType=BIGINT}

  </update>
  <update id="updateworkUsersStatus" parameterType="java.lang.Long" >

    update bags
    set
    status = "2"
    where id=#{id,jdbcType=BIGINT}

  </update>


  <select id="warehousebagsFanAll" resultType="com.zeus.entity.Bags">
  SELECT
    <include refid="Base_Column_List"/>
   FROM bags WHERE bags.`id`
    IN(SELECT bags_id FROM warehouse_bags WHERE warehouse_id = #{id,jdbcType=BIGINT})
   AND DATE(update_time) = #{date,jdbcType=VARCHAR} AND STATUS = '1';
  </select>

  <select id="warehousebagsOkFanAll" resultType="com.zeus.entity.Bags">
    SELECT
    <include refid="Base_Column_List"/>
    FROM bags WHERE bags.`id`
    IN(SELECT bags_id FROM warehouse_bags WHERE warehouse_id = #{id,jdbcType=BIGINT})
    AND DATE(update_time) = #{date,jdbcType=VARCHAR} AND STATUS = '2';
  </select>


</mapper>